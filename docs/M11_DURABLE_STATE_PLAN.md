# MCP-M11 Durable State

This branch contains the durable YDB state work. It must not be treated as production-ready until rebased onto the latest main, covered by deterministic tests, and passed through CI. DuckDB remains local/reference only because Yandex Serverless Container local filesystem is ephemeral.
